# 防死循环与上下文保护（本项目长期有效）

本规则用于防止 Codex 在复杂任务中反复刷英文、原地打转、无谓消耗上下文。与本项目内其他说明冲突时，本规则优先。

## 输出纪律

1. 所有面向用户的输出一律使用简体中文；工具输出里的英文报错不用整段复述。
2. 这是项目铁律：同一句话、同一个动作、同一种“我准备做…”的声明在连续消息里重复出现达到 2 次，立即中断当前思路，禁止继续复述。
3. 执行优先于描述：需要执行的操作直接调用工具，不要用多轮消息反复表达“接下来我要做 X”。

## 执行纪律

4. 复杂任务先建立 `update_plan`，每完成一步立即更新状态，禁止在同一状态原地打转。
5. 工具输出只保留必要信息：长日志、英文报错使用输出截断或 `Select-Object -First N` 只取关键行，禁止把整段输出反复贴进对话。
6. 同一个阻塞点最多尝试 2 次：第 1 次尝试修复，第 2 次调整方案；仍失败就停止自行重试，向用户一次性说明问题、可选方案与推荐做法。
7. 遇到关键数据缺失、外部权限、破坏性操作、方向性变更时停下来问用户，不要靠大量重复输出“推进”。

## 上下文保护

8. 不为显示进度而大量输出中间过程；阶段简报和最终交付保持精简。
9. 一个大任务交付完成后主动建议开启新任务，避免把长期对话的上下文越拖越大。
